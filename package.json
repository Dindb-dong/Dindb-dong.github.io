{
  "name": "dindbos-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "build": "vite build && node scripts/copy-static.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test:e2e": "playwright test",
    "qa": "npm run build && npm run test:e2e",
    "browser:install": "npm --prefix remote-browser install",
    "browser:start": "npm --prefix remote-browser start",
    "dev:full": "node scripts/dev.mjs"
  },
  "dependencies": {
    "dindbos": "0.1.1",
    "vite": "^5.4.21"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@vitejs/plugin-react": "^4.3.4"
  }
}
